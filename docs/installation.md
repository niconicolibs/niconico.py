# インストール

## 要件

- Python 3.11 以降
- 動画ダウンロードを利用する場合は FFmpeg

## パッケージのインストール

```bash
pip install niconico.py
```

## FFmpeg

動画ダウンロードでは FFmpeg を利用します。
`ffmpeg` コマンドを PATH から実行できる状態にしてください。

```bash
ffmpeg -version
```
